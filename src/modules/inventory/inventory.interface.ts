export interface TInventory {
  name: string;
  quantity: number;
  price: number;
  category: string;
  brand: string;
  compatibility: string;
  interface: string;
  condition: string;
  capacity: string;
  color: string;
}
